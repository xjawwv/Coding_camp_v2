import { createError, readMultipartFormData } from 'h3'
import { randomUUID } from 'node:crypto'
import { requireAdmin } from '../../../utils/auth'
import { database } from '../../../utils/db'

export default defineEventHandler(async event => { await requireAdmin(event); const parts = await readMultipartFormData(event) || []; const body = Object.fromEntries(parts.filter(part => !part.filename).map(part => [part.name, part.data.toString()])); const image = parts.find(part => part.name === 'image'); const required = ['title', 'description', 'learningObjectives', 'level', 'category', 'duration', 'uploadedAt']; if (required.some(key => !body[key]?.trim()) || !image?.filename || !image.type?.startsWith('image/')) throw createError({ statusCode: 400, statusMessage: 'Semua field course dan gambar valid wajib diisi' }); if (image.data.length > 2 * 1024 * 1024) throw createError({ statusCode: 413, statusMessage: 'Ukuran gambar maksimal 2 MB' }); const imageData = `data:${image.type};base64,${image.data.toString('base64')}`; await database().query('INSERT INTO courses (id, title, description, learning_objectives, image, level, category, duration, uploaded_at) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)', [randomUUID(), body.title.trim(), body.description.trim(), body.learningObjectives.trim(), imageData, body.level, body.category.trim(), body.duration.trim(), body.uploadedAt]); return { ok: true } })
