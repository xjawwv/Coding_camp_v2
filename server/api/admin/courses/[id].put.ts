import { createError, readMultipartFormData } from 'h3'
import { requireAdmin } from '../../../utils/auth'
import { database } from '../../../utils/db'

export default defineEventHandler(async event => { await requireAdmin(event); const id = getRouterParam(event, 'id'); const parts = await readMultipartFormData(event) || []; const body = Object.fromEntries(parts.filter(part => !part.filename).map(part => [part.name, part.data.toString()])); const image = parts.find(part => part.name === 'image'); if (!id || !body.title?.trim() || !body.description?.trim() || !body.learningObjectives?.trim() || !body.level || !body.category?.trim() || !body.duration?.trim() || !body.uploadedAt) throw createError({ statusCode: 400, statusMessage: 'Data course tidak valid' }); if (image?.filename && (!image.type?.startsWith('image/') || image.data.length > 2 * 1024 * 1024)) throw createError({ statusCode: 400, statusMessage: 'Gambar harus berupa image maksimal 2 MB' }); const values = [body.title.trim(), body.description.trim(), body.learningObjectives.trim(), body.level, body.category.trim(), body.duration.trim(), body.uploadedAt, id]; if (image?.filename) { values.splice(3, 0, `data:${image.type};base64,${image.data.toString('base64')}`); await database().query('UPDATE courses SET title = ?, description = ?, learning_objectives = ?, image = ?, level = ?, category = ?, duration = ?, uploaded_at = ? WHERE id = ?', [body.title.trim(), body.description.trim(), body.learningObjectives.trim(), values[3], body.level, body.category.trim(), body.duration.trim(), body.uploadedAt, id]) } else await database().query('UPDATE courses SET title = ?, description = ?, learning_objectives = ?, level = ?, category = ?, duration = ?, uploaded_at = ? WHERE id = ?', values); return { ok: true } })
